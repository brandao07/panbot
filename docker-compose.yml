services:
  panbot:
    build: .
    container_name: panbot
    ports:
      - "8080:8080"
    volumes:
      - .:/app
      - /app/tmp
    networks:
      - panbot-network

networks:
  panbot-network:
    driver: bridge
